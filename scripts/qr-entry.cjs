const QR=require('../vendor/qr/index.js');window.makeRoomQR=function(canvas,value){const qr=new QR(-1,1);qr.addData(value);qr.make();const n=qr.getModuleCount(),scale=8;canvas.width=canvas.height=(n+8)*scale;const ctx=canvas.getContext('2d');ctx.fillStyle='white';ctx.fillRect(0,0,canvas.width,canvas.height);ctx.fillStyle='#152c51';for(let y=0;y<n;y++)for(let x=0;x<n;x++)if(qr.isDark(y,x))ctx.fillRect((x+4)*scale,(y+4)*scale,scale,scale);};
